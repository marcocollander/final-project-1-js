class Model {
  construktor(inputName, inputPrice, inputSum, btnAdd) {
    this.inputName = inputName;
    this.inputPrice = inputPrice;
    this.inputSum = inputSum;
    this.btnAdd = btnAdd;
  }

  setInput(inputName, querySelector) {
    this.inputName = documet.querySelector(selctor)
  }
}